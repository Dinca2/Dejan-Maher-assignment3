#include "DoublyLinkedList.h"

int main(int argc, char* argv[]) {
  /* 
  DoublyLinkedList<int> list;
  int item = 1;
  cout << "testint insert: " << endl;
  list.insertItem(item);
  item = 5;
  list.insertItem(item);
  item = 3;
  list.insertItem(item);
  item = 3;
  list.insertItem(item);
  list.print();
  cout << endl;
  cout << "done" << endl;

  cout << endl;
  
  cout << "testint print reverse: " << endl;
  list.printReverse();
  cout << endl;
  cout << "done" << endl;
  
  cout << endl;

  cout << "testing delete: " << endl;
  list.print();
  cout << endl;
  item = 1;
  list.deleteItem(item);
  list.print();
  cout << endl;
  item = 2;
  list.deleteItem(item);
  item = 5;
  list.deleteItem(item);
  list.print();
  cout << endl;
  item = 3;
  list.deleteItem(item);
  list.print();
  cout << endl;
  item = 3;
  list.deleteItem(item);
  list.print();
  cout << endl;
  list.deleteItem(item);
  cout << "done" << endl;

  cout << endl;

  cout << "testing inserting in empty list: " << endl;
  item = 5;
  list.insertItem(item);
  list.print();
  cout << endl;
  cout << "done" << endl;

  cout << endl;
  
  cout << "testing deleteSubsection: " << endl;
  item = 1;
  list.insertItem(item);
  item = 2;
  list.insertItem(item);
  item = 3;
  list.insertItem(item);
  item = 5;
  list.insertItem(item);
  item = 6;
  list.insertItem(item);
  item = 7;
  list.insertItem(item);
  cout << "Orignial: ";
  list.print();
  cout << endl;
  //list.deleteSubsection(10, 1); //deletes all
  list.deleteSubsection(6, 2); //deletes mid section
  //list.deleteSubsection(4, 1); //deletes beginning section
  //list.deleteSubsection(7, 4); //deletes back section
  cout << "Modified: ";
  list.print();
  cout << endl;
  cout << "done" << endl;

  cout << endl;

  cout << "testing mode: " << endl;
  //  item = 1;
  //list.insertItem(item);
  item = 2;
  list.insertItem(item);
  item = 3;
  list.insertItem(item);
  item = 5;
  list.insertItem(item);
  item = 6;
  list.insertItem(item);
  item = 7;
  list.insertItem(item);
  //list.insertItem(item);
  //list.deleteSubsection(7,1); test for empty list
  list.print();
  cout << endl;
  if(list.lengthIs() == 0) {
    cout << "mode: " << endl;
  } else {
    cout << "mode: " << list.mode() << endl;
  }
  cout << "done" << endl;

  cout << endl;

  cout << "testing swapAlt: " << endl;
  list.print();
  cout << endl;
  list.swapAlternate();
  list.print();
  cout << endl;
  cout << "done" << endl;
  */
  return EXIT_SUCCESS;
}
